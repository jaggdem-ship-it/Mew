import { HealthOrb } from '../ui/HealthOrb.js'  ;  import { XPBar } from '../ui/XPBar.js'  ;  export class UIScene extends Phaser.Scene { constructor() { super({ key: 'UIScene' }); } init(data) { this.runState = data.runState; } create() { const w = this.scale.width; const h = this.scale.height; this.healthOrb = new HealthOrb(this, 30, h - 25); this.xpBar = new XPBar(this, 60, h - 12, w - 70, 6); this.weaponIcons = []; } update() { const levelScene = this.scene.get('LevelScene'); if (!levelScene || !levelScene.player || levelScene.player.dead) return; const player = levelScene.player; const progression = levelScene.progression; this.healthOrb.update(player.getHpPercent()); if (progression) { this.xpBar.update( progression.xp / progression.xpToNext, progression.level ); } const weapons = Object.entries(this.runState.weapons || {}); while (this.weaponIcons.length < weapons.length) { const icon = this.add.circle(70 + this.weaponIcons.length * 18, this.scale.height - 35, 6, 0x666666); icon.setDepth(10); const txt = this.add.text(70 + this.weaponIcons.length * 18, this.scale.height - 28, '', { fontSize: '8px', color: '#ffffff' }).setOrigin(0.5).setDepth(11); this.weaponIcons.push({ icon, txt }); } weapons.forEach(([key, lvl], i) => { if (i < this.weaponIcons.length) { const def = this.runState.weaponDefs[key]; this.weaponIcons[i].icon.setFillStyle(def ? 0xff6666 : 0x666666); this.weaponIcons[i].txt.setText(`${lvl}`); } }); } }