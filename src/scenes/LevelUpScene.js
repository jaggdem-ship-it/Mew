export class LevelUpScene extends Phaser.Scene {
  constructor() { super({ key: "LevelUpScene" }); }

  create() {
    // Level up is handled within LevelScene via CardPanel
    // This scene exists as a placeholder for potential future expansion
  }
}
